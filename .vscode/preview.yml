# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 8080 # 应用的端口
    run: npm install && npm run docs:dev # 应用的启动命令
    root: ./ # 应用的启动目录
    name: Tsanfer's blog # 应用名称
    description: 个人博客 # 应用描述
    autoOpen: true # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
